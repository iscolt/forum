server:
  port: 8080
spring:
  output:
    ansi:
      enabled: always
  datasource:
    username: root
    password: 5211
    url: jdbc:mysql://39.107.101.237:3306/forum?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver
# mybatis 扫包
mybatis:
  mapper-locations:
    classpath*: mapper/*.xml
  # 开启驼峰命名转换
  configuration:
    map-underscore-to-camel-case: true